trigger:
  - azure-pipeline
pool:
  vmImage: 'ubuntu-latest'
stages:
  - stage: Build
    jobs:
      - job:
        displayName: Build and Run Tests
        steps:
#          - task: Docker@2
#            displayName: Build an image
#            inputs:
#              repository: $(imageName)
#              command: build
#              Dockerfile: Dockerfile
          - task: DockerCompose@0
            displayName: Run Docker Command
            inputs:
              containerregistrytype: Container Registry
              dockerComposeFile: docker-compose.yml
              dockerComposeCommand: 'up -d'
          - task: Maven@3
            inputs:
              mavenPomFile: 'pom.xml'
              goals: 'test'
              options: 'clean test -Dsurefire.suiteXmLFiles=regression.xml'
              publishJUnitResults: true
              testResultsFiles: '**/surefire-reports/*.xml'
              javaHomeOption: 'JDKVersion'
              mavenVersionOption: 'Default'
              mavenAuthenticateFeed: false
              effectivePomSkip: false
              sonarQubeRunAnalysis: false
              sqMavenPluginVersionChoice: 'latest'
#          - task: SonarQubePrepare@5
#            inputs:
#              SonarQube: 'sonarqube'
#              scannerMode: CLI
#              configFile: sonar-project.properties
#          - task: SonarQubeAnalyze@4
#          - task: SonarQubePublish@5
#            inputs:
#              pollingTimeoutSec: '300'

#clean test -Dsurefire.suiteXmLFiles=regression.xml sonar:sonar -Dsonar.login=65da9114f5f17e274abe3a7a1af0b79dd81159ab